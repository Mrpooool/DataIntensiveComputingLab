# 项目执行方案

依据：[课程要求](Assignment.md)。W1 已完成代码与交付包；当前进入 W2 方案阶段，以下 W2 项目均待实现和验收。

## W1 完成总结

- 已实现四份原始数据的通用摄入、标准化、校验、去重和 Delta 存储，并生成逐趟整合表 `integrated_taxi_trips`，包含天气、PM2.5、上下车区域和 borough。
- 已有验证记录：2026-09-09 的 27 项小样本测试通过；独立全量运行保留 9,554,576 条唯一行程，S0/S1 存储实验完成并验证查询结果一致。这里记录历史结果，不代表 W2 已验证。
- W1 提交包已放入 `main`：`submissions/Week1_submission_2026-09-13.zip`。设计与规则详见 [设计报告](docs/w1_design_report.md)、[数据契约](docs/data_contract.md)、[benchmark report](docs/benchmark_report.md)；历史报告与最终重跑的数值分别保留，不混用。
- W2 继续使用 Python 3.11.9、JDK 21 和 `requirements.txt` 的固定版本。读取同一成功批次的四张基础 Delta 表及整合表，保留 W1 入口和数据。
- 沿用 UTC 存储、纽约当地时间分析、同小时环境关联和缺测保留规则。现有 Taxi 数据覆盖 2024 年 1–3 月；天气来源时区假设、环境数据的空间局限继续适用。

## W2 目标与分工

目标：实现六个 Spark SQL 分析查询，生成至少四张可复用汇总 Delta 表，完成四类优化的对照实验，并提交可复现的英文文档。

| 负责人 | 主要任务 | 交付与职责边界 |
| --- | --- | --- |
| A：数据产品与运行入口 | 自动生成四张汇总表，管理存储、刷新元数据、CLI 和配置 | 产品生成代码及写入/读回测试；README、最终运行入口和材料整合 |
| B：分析口径与查询 | 定义统计规则，实现全部六个 Spark SQL 查询及产品聚合逻辑 | 查询库、小样本正确性测试；设计报告中的分析要求、查询与产品设计 |
| C：优化与 benchmark | 研究缓存、分区裁剪、广播连接、AQE，核对结果并记录性能 | 实验代码、耗时与执行计划、benchmark report；设计报告中的优化策略和权衡 |

各自维护对应测试；公共配置由 A 汇总，统计口径由 B 维护，实验口径由 C 维护。A 负责最终联调和材料汇总，避免全部交付工作集中到 C。

## 1. 分析口径与六个查询（B）

| 编号 | 分析问题 | 基础口径与输出 |
| --- | --- | --- |
| Q1 | 各 Taxi Zone 的月度需求 | 按纽约当地月份、上车 Zone 统计行程数 |
| Q2 | 不同天气下的平均距离 | 按天气类别统计有效距离的均值、有效样本数和总行程数 |
| Q3 | 空气质量与需求的关系 | 先得到每小时订单量，再分析 PM2.5 与订单量的关联，输出分组统计及相关系数 |
| Q4 | 天气下需求变化最大的 Zone | 对各 Zone 计算不同天气类别下的每小时平均订单量，以类别间最大值减最小值排名，并报告样本小时数 |
| Q5 | 一周各日的高峰小时 | 按当地星期、小时计算平均每小时订单量，输出各星期的高峰时段，保留并列 |
| Q6 | 整体月度需求趋势 | 按月统计订单量及环比；首月环比为空，注明仅覆盖三个月 |

共同规则：

- 需求统一指上车订单数；日期区间采用起始日包含、结束日不包含。月份、星期和钟点使用 `America/New_York`，实际小时键保留 UTC，避免夏令时重复小时合并。
- 天气分类先核查代码含义；无法确认时按原始天气代码分组，不编造“晴/雨/雪”标签。PM2.5 分组边界由 B 在首阶段固定并写入配置，不将浓度冒充 AQI。
- 环境影响分析仅使用纽约适用范围。缺测和未知区域单独报告；缺少环境指标的小时不参与对应相关系数，不能填成零。
- Q3–Q5 使用完整有效时间范围补齐零订单小时；Q4 还需补齐 Zone × 小时，避免只统计有订单的小时。天气缺测与零订单是不同情况。
- Q4 至少需两个有效天气类别；各类别最低样本小时数在实验前固定。Q3、Q4 描述关联，不作因果结论。
- 产品和查询复用同一套 SQL 聚合口径；字段、单位、时间或关联规则发生变化时更新 [数据契约](docs/data_contract.md)。

## 2. 可复用数据产品（A 落盘，B 定义聚合）

以下表名为拟定接口，首阶段确认后固定。所有产品由整合表自动生成。

| 产品 | 粒度与主要指标 | 使用者与物化价值 |
| --- | --- | --- |
| `daily_mobility_summary` | 当地日期 × 小时；订单数、距离/时长之和及有效样本数 | 运营分析人员；支持高峰与月度趋势，减少反复扫描逐趟数据 |
| `taxi_zone_statistics` | 月份 × 上车 Zone；订单数、距离/车费之和及有效样本数 | 区域规划人员；直接比较区域需求 |
| `weather_impact_summary` | 上车 Zone × 天气类别；订单数、有效小时数、距离之和及有效样本数 | 天气影响分析人员；复用天气分类与小时需求统计 |
| `air_quality_impact_summary` | UTC 小时；当地日期、PM2.5、订单数和匹配状态 | 环境分析人员；直接分析小时环境与需求关系 |

- 存储路径拟为 `data/delta/analytics/<product>/`；小型汇总表默认不分区，确需分区时用实测说明收益。
- 产品保留求和与计数，重新聚合时用总和除以总有效样本数，不能直接平均各组均值。
- 元数据至少包括产品名、数据来源及源表版本、创建时间、最近成功刷新时间、Schema 版本和刷新状态。首次创建时间保留，成功刷新后更新刷新时间。
- W2 先提供可重复执行的全量刷新；同一输入重复运行不累计数据。读回校验通过后记录成功，失败不得标为已刷新。增量刷新留给 W3。

## 3. 优化与实验（C）

六个查询均保留基础版本和优化版本的结果、耗时与执行计划。四类技术至少各完成一个适用查询的独立对照；不适用的组合说明原因。

| 优化 | 实验设计 | 必须检查的证据 |
| --- | --- | --- |
| 缓存 | 对重复扫描整合表或小时中间结果的查询比较缓存前后耗时 | 分别记录缓存构建成本、复用耗时和内存代价；确认计划读取缓存，实验后清理 |
| 分区裁剪 | 对同一日期范围，比较原查询与显式包含 `pickup_date` 条件的等价写法 | 检查 `PartitionFilters` 和扫描范围；不能用不同日期范围制造加速 |
| 广播连接 | 从基础 Taxi 与小型 Zone/环境表构建等价分析，对比普通连接与广播连接 | 确认右表键唯一、计划真实改变；控制自动广播及 AQE 转换，避免两组实际上都是广播 |
| AQE | 对含聚合或连接 shuffle 的查询，单独比较 AQE 开关 | 固定其余配置，保存执行后的最终计划，记录分区合并或连接策略变化 |

实验统一要求：

- 固定机器、资源、输入快照、日期范围和其他 Spark 设置；一次只改变一个因素。先完成独立对照，再测组合优化。
- 每组预热一次、正式测量至少三次，交替执行顺序，保存原始耗时和中位数。缓存构建单独计时，不把 OS 缓存不可控的实验称为冷缓存实验。
- 对小型聚合结果使用 `collect()` 完整计算，不能只构造 DataFrame 或对结果 `count()` 代替实际查询。
- 比较键集合、行数和指标：计数精确相等，浮点使用预先固定的容差，正确处理空值与并列排名；核对通过后才能报告加速比。
- 保存 SQL、参数、`EXPLAIN FORMATTED`、Spark 配置和结果一致性状态，拟写入 `data/benchmark/w2/<run_id>/`。
- 对产品查询另测生成/刷新耗时、查询耗时及当前 Delta 快照的数据字节数与文件数；日志和历史文件开销单列。
- 报告解释收益最大、无明显收益和仍然昂贵的查询，结合数据规模、分区和分布说明原因，并讨论扩展到十个城市时的调整。不预设任何优化一定更快。

## 4. 三个交接接口

| 交接 | 首阶段需要固定的内容 |
| --- | --- |
| B → A/C：查询接口 | Q1–Q6 的 SQL、表/视图名、日期参数、结果字段及类型、空值和排序规则；产品复用的聚合 SQL |
| A → B/C：数据接口 | 成功批次与 Delta 版本、四张基础表和整合表的注册入口、产品路径与元数据读取方式 |
| C → A/B：实验接口 | 查询编号、实验开关、计时边界、浮点容差、结果/计划文件格式及复现入口 |

建议新增 `queries.py`（B）、`data_products.py`（A）、`query_benchmark.py`（C），放在 `src/dic_pipeline/`；A 汇总对应 `scripts/` 入口。用普通函数和清晰的 SQL 文件或字符串组织，不额外搭建框架。

## 5. 推进顺序与验收

| 阶段 | A | B | C | 完成标志 |
| --- | --- | --- | --- | --- |
| ① 接口与最小链路 | 注册输入表，生成一张产品 | 固定口径，先完成 Q1 | 用 Q1 跑通一组对照 | 一个查询、一张产品、一组实验可端到端运行 |
| ② 并行实现 | 完成四张产品和元数据 | 完成六个 SQL 及边界测试 | 完成四类优化实验框架 | 模块测试通过，查询与产品口径一致 |
| ③ 全量评估 | 执行刷新并测存储开销 | 复核统计结果和缺测影响 | 运行全量对照，保存耗时与计划 | 六个查询结果一致，四类优化均有实测证据 |
| ④ 交付 | 整合入口、README 和提交包 | 完成分析及产品设计说明 | 完成优化说明和 benchmark report | 按 README 在独立输出目录可复现 |

当前进度：A 的四张产品和 B 的六个查询已实现并通过小样本测试；C 的优化实验及三方全量联调仍待完成。

- [x] 六个 Spark SQL 查询齐全，统计口径和小样本预期结果明确。
- [ ] 至少四张产品 Delta 表可自动生成、读回和重复刷新，元数据完整。
- [ ] 缓存、分区裁剪、广播连接、AQE 四类实验完成，优化前后结果一致。
- [ ] 六个查询的耗时与执行计划、适用产品的存储开销已记录。
- [ ] 受影响测试通过；修改共享模块时跑完整测试；交付前通过 `git diff --check`。
- [ ] 提交完整代码/配置/测试、3–5 页英文设计报告、简短英文 benchmark report 和简洁英文 README；同步中文 README。
- [ ] README 覆盖查询运行、产品生成和实验复现；A 汇总，B/C 分别审核自己负责的内容。
